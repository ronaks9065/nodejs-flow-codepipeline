version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10  
    commands:
      - npm install -g mocha
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm test
  post_build:
    commands:
      - echo Nothing
